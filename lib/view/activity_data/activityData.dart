import 'package:flutter/material.dart';

class ActivityData extends StatelessWidget {
  const ActivityData({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("activity Data"),),
      body: ListTile(),
    );
  }
}